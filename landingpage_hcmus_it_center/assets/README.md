# Assets Directory

Thư mục chứa các tài nguyên tĩnh như hình ảnh, biểu tượng (nếu có).
Website hiện tại ưu tiên sử dụng CSS thuần và SVG inline/data URI để đảm bảo nhẹ, tải nhanh và không phụ thuộc ảnh bên ngoài.
